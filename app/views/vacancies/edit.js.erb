// Add the dialog title
$('#dialog h4').html("Изменить статус вакансии");

// Render the new form
$('.modal-body').html('<%= j render('form_status') %>');

// Show the dynamic dialog
$('#dialog').modal("show");