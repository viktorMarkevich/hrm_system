$('.vacancy-candidates').html('<%= escape_javascript(render partial: 'vacancy_candidates',
                                                            locals: { candidates: @candidates_with_found_status,
                                                                      vacancy: @vacancy }) %>')