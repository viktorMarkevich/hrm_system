%table.table.table-bordered.table-hover
  %thead
    %tr
      %th Название
      %th Зарплата
      %th Статус
  %tbody
    - vacancies.each do |v|
      %tr
        %td= v.name
        %td= get_salary(v)
        %td
          %span{class: "label #{get_label_class(v)}"}= v.status