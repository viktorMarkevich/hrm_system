.sticker
  .sticker-title
    = link_to sticker.title, sticker_path(sticker)
    .sticker-toolbar
      %span.edit-sticker
        = link_to 'Edit', edit_sticker_path(sticker)
      |
      %span.remove-sticker
        = link_to 'x', sticker_path(sticker), method: :delete,  data: { confirm: 'Вы действительно хотите удалить стикер?' }
  .sticker-desc
    = sticker.description