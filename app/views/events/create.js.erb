$('#dialog').modal('toggle');

$('.items-list').html('<%= escape_javascript(render partial: 'events_list',
                                                  locals: { events: events_current_month(params[:start_date]) }) %>')
$('.calendar').html('<%= escape_javascript(render partial: 'calendar',
                                                  locals: { events: @events }) %>')
$('.modal-backdrop').remove();