// Add the dialog title
$('#dialog h3').html("Добавить событие");

// Render the new form
$('.modal-body.form').html('<%= j render('form') %>');

// Show the dynamic dialog
$('#dialog').modal("show");
$('.modal .fade .in').modal("hide");