// Add the dialog title
$('#dialog h3').html("События");

// Render the new form
$('.modal-body').html('<%= j render partial: 'events_list', locals: { events: @events, time_is: :future } %>');

// Show the dynamic dialog
$('#dialog').modal("show");
//$('.modal .fade .in').modal("hide");