$('.events-list').remove();
$('.modal-backdrop').remove();
// Add the dialog title

$('#dialog h3').html("Редактировать событие");

// Render the new form
$('.modal-body.form').html('<%= j render('form') %>');

// Show the dynamic dialog
$('#dialog').modal("show");

// Set focus to the first element
$('#dialog').on('shown.bs.modal', function () {
    $('.first_input').focus()
})