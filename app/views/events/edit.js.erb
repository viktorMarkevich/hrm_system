//$('.modal').hide();
//$('.modal-backdrop').remove();
// Add the dialog title

$('#dialog h3').html("Редактировать событие");

// Render the new form

<%- if %w(Собеседование Утвержден).include?(@event.try(:name))%>
    $('.modal-body').html('<%= j render partial: 'edit_staff_relation_form', locals: { events: @events } %>');
<%- else %>
    $('.modal-body').html('<%= j render partial: 'edit_own_form', locals: { events: @events } %>');
<%end %>

// Show the dynamic dialog
$('#dialog').modal("show");
